<%= form_with(model:[:admin, @user], local: true ) do |form| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this task from being saved:</h2>
      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
   <br>
  <div class="field">
    <%= form.text_field :name, placeholder: 'name', class:"form-control" %>
    <%= form.email_field :email, placeholder: 'email', class:"form-control" %>
    <%= form.password_field :password, placeholder: 'password', class:"form-control" %>
    <%= form.password_field :password_confirmation, placeholder: 'password_confirmation' , class:"form-control" %>
    <%= form.label :admin %>
    <%= form.check_box :admin, {}, 'true', 'false' %>
    <%= form.submit "submit", class:"button_primary" %>
    <% end %>
  </div>
